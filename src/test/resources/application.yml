# Test
spring:
  profiles:
    active: local # 기본 환경 선택
server:
  servlet:
    encoding:
      force-response: true

# local 환경
---
spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true